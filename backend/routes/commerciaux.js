// ROUTES COMMERCIAUX - API REST pour les commerciaux
// GET / POST / PUT / DELETE
﻿const router = require("express").Router(); const Commercial = require("../models/Commercial"); router.get("/", async (req, res) => { try { const data = await Commercial.find().sort({createdAt:-1}); res.json(data); } catch (err) { res.status(500).json({ error: err.message }); } }); router.post("/", async (req, res) => { try { const doc = new Commercial(req.body); await doc.save(); res.json(doc); } catch (err) { res.status(500).json({ error: err.message }); } }); router.put("/:id", async (req, res) => { try { const doc = await Commercial.findByIdAndUpdate(req.params.id, req.body, { new: true }); res.json(doc); } catch (err) { res.status(500).json({ error: err.message }); } }); router.delete("/:id", async (req, res) => { try { await Commercial.findByIdAndDelete(req.params.id); res.json({ message: "Supprime" }); } catch (err) { res.status(500).json({ error: err.message }); } }); module.exports = router;