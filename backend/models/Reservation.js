// MODELE RESERVATION - Reservations de places pour les evenements
// Gestion des statuts : confirmee, annulee, en attente
﻿const mongoose = require("mongoose"); const ReservationSchema = new mongoose.Schema({ client: { type: mongoose.Schema.Types.ObjectId, ref: "Client" }, evenement: { type: mongoose.Schema.Types.ObjectId, ref: "Evenement" }, categorie: String, nombrePlaces: Number, montantTotal: Number, statut: { type: String, enum: ["confirmee", "annulee", "en_attente"], default: "en_attente" }, dateReservation: { type: Date, default: Date.now } }, { timestamps: true }); module.exports = mongoose.model("Reservation", ReservationSchema);