import React, { useEffect, useState } from "react"; import { getVentes, createVente, deleteVente, getClients, getProduits, getCommerciaux } from "../api"; export default function Ventes() { const [items, setItems] = useState([]); const [clients, setClients] = useState([]); const [produits, setProduits] = useState([]); const [commerciaux, setCommerciaux] = useState([]); const [form, setForm] = useState({ client: "", produit: "", quantite: "", prixUnitaire: "", commercialId: "" }); useEffect(() => { getVentes().then(r => setItems(r.data)); getClients().then(r => setClients(r.data)); getProduits().then(r => setProduits(r.data)); getCommerciaux().then(r => setCommerciaux(r.data)); }, []); const handleSubmit = async (e) => { e.preventDefault(); await createVente(form); const r = await getVentes(); setItems(r.data); }; const handleDelete = async (id) => { await deleteVente(id); setItems(items.filter(i => i._id !== id)); }; return ( <div style={{padding:"2rem"}}> <h2>Gestion des Ventes</h2> <form onSubmit={handleSubmit} style={{display:"flex", flexWrap:"wrap", gap:"0.5rem", marginBottom:"2rem"}}> <select value={form.client} onChange={e => setForm({...form, client: e.target.value})} style={{padding:"0.5rem", borderRadius:"0.5rem", border:"1px solid #ccc"}}><option value="">Client</option>{clients.map(c => <option key={c._id} value={c._id}>{c.nom}</option>)}</select> <select value={form.produit} onChange={e => setForm({...form, produit: e.target.value})} style={{padding:"0.5rem", borderRadius:"0.5rem", border:"1px solid #ccc"}}><option value="">Produit</option>{produits.map(p => <option key={p._id} value={p._id}>{p.nom}</option>)}</select> <input placeholder="Quantite" value={form.quantite} onChange={e => setForm({...form, quantite: e.target.value})} style={{padding:"0.5rem", borderRadius:"0.5rem", border:"1px solid #ccc"}} /> <input placeholder="Prix unitaire" value={form.prixUnitaire} onChange={e => setForm({...form, prixUnitaire: e.target.value})} style={{padding:"0.5rem", borderRadius:"0.5rem", border:"1px solid #ccc"}} /> <select value={form.commercialId} onChange={e => setForm({...form, commercialId: e.target.value})} style={{padding:"0.5rem", borderRadius:"0.5rem", border:"1px solid #ccc"}}><option value="">Commercial</option>{commerciaux.map(c => <option key={c._id} value={c._id}>{c.nom}</option>)}</select> <button type="submit" style={{padding:"0.5rem 1rem", background:"#0891b2", color:"white", border:"none", borderRadius:"0.5rem", cursor:"pointer"}}>Ajouter</button> </form> <table style={{width:"100%", borderCollapse:"collapse"}}> <thead><tr style={{background:"#f3f4f6"}}><th style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>Client</th><th style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>Produit</th><th style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>Quantite</th><th style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>Prix</th><th style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>Actions</th></tr></thead> <tbody>{items.map(i => <tr key={i._id}><td style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>{i.client?.nom}</td><td style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>{i.produit?.nom}</td><td style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>{i.quantite}</td><td style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}>{i.prixUnitaire} </td><td style={{padding:"0.5rem", border:"1px solid #e5e7eb"}}><button onClick={() => handleDelete(i._id)} style={{background:"#ef4444", color:"white", border:"none", borderRadius:"0.5rem", padding:"0.3rem 0.7rem", cursor:"pointer"}}>Supprimer</button></td></tr>)}</tbody> </table> </div> ); }